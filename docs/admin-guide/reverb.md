# Laravel reverb
G-nom uses [Laravel Reverb](https://reverb.laravel.com/) to send realtime notification e.g. updates on the progress of data imports to users. The reverb server is started using artisan in the G-nom installation directory:

```bash
php artisan reverb:start
```